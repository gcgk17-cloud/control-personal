'use client'
import {useEffect,useState} from 'react';import AuthGate from '@/components/AuthGate';import {supabase} from '@/lib/supabase';import Link from 'next/link'
export default function Home(){const[s,setS]=useState<any>({});useEffect(()=>{(async()=>{const start=new Date();start.setHours(0,0,0,0);const[w,m,r,t,g,i]=await Promise.all([supabase.from('weight_logs').select('weight_kg').order('measured_at',{ascending:false}).limit(1),supabase.from('meals').select('calories').gte('eaten_at',start.toISOString()),supabase.from('running_logs').select('distance_km,calories').gte('run_at',start.toISOString()),supabase.from('transactions').select('type,amount'),supabase.from('gym_logs').select('id').gte('trained_at',start.toISOString()),supabase.from('installments').select('total_amount,months,paid_months')]);const tx=t.data||[],runs=r.data||[];setS({weight:w.data?.[0]?.weight_kg,cal:(m.data||[]).reduce((a,x)=>a+Number(x.calories),0),km:runs.reduce((a,x)=>a+Number(x.distance_km),0),burn:runs.reduce((a,x)=>a+Number(x.calories||0),0),money:tx.reduce((a,x)=>a+(x.type==='ingreso'?Number(x.amount):-Number(x.amount)),0),gym:g.data?.length||0,msi:(i.data||[]).reduce((a,x)=>a+(Number(x.paid_months)<Number(x.months)?Number(x.total_amount)/Number(x.months):0),0)})})()},[]);const remaining=2200-(s.cal||0);return <AuthGate><h1>Hola 👋</h1><p className="muted">Tu resumen personal de hoy.</p><div className="grid"><div className="card">💰 Balance movimientos<div className="kpi">${(s.money||0).toLocaleString('es-MX',{minimumFractionDigits:2})}</div></div><div className="card">🔥 Calorías<div className="kpi">{Math.round(s.cal||0)} / 2200</div><span className="muted">Consumidas hoy</span></div><div className="card">⚖️ Peso<div className="kpi">{s.weight??'—'} kg</div><span className="muted">Meta 80–82 kg</span></div></div><div className="quick"><Link className="btn" href="/finanzas">＋ Gasto / ingreso</Link><Link className="btn" href="/alimentacion">＋ Comida</Link><Link className="btn" href="/peso">＋ Peso</Link><Link className="btn" href="/running">＋ Running</Link></div><div className="grid"><div className="card"><h3>🏃 Entrenamiento</h3><div className="kpi">{(s.km||0).toFixed(1)} km</div><p className="muted">Hoy · Gym: {s.gym||0} registros</p></div><div className="card"><h3>🍎 Balance del día</h3><div className="kpi small">{Math.abs(Math.round(remaining))} kcal</div><p className="muted">{remaining>=0?'disponibles para hoy':'por encima de la meta inicial'}</p></div><div className="card"><h3>💳 Compromisos</h3><div className="kpi small">${(s.msi||0).toLocaleString('es-MX',{maximumFractionDigits:0})}/mes</div><p className="muted">Mensualidades MSI activas estimadas.</p></div></div></AuthGate>}
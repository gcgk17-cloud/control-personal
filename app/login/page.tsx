'use client'
import {useState} from 'react'; import {supabase} from '@/lib/supabase'; import {useRouter} from 'next/navigation'
export default function Login(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[msg,setMsg]=useState('');const r=useRouter();
async function entrar(){setMsg('');const{error}=await supabase.auth.signInWithPassword({email,password});if(error)setMsg(error.message);else r.push('/')}
async function crear(){setMsg('');const{error}=await supabase.auth.signUp({email,password});setMsg(error?error.message:'Cuenta creada. Si Supabase solicita confirmación, revisa tu correo; después inicia sesión.')}
return <><h1>🔐 Acceso</h1><div className="card form"><label>Correo</label><input className="input" placeholder="correo@ejemplo.com" value={email} onChange={e=>setEmail(e.target.value)}/><label>Contraseña</label><input className="input" type="password" placeholder="Mínimo 6 caracteres" value={password} onChange={e=>setPassword(e.target.value)}/><div className="quick"><button className="btn" onClick={entrar}>Entrar</button><button className="btn secondary" onClick={crear}>Crear cuenta</button></div>{msg&&<p>{msg}</p>}</div></> }
'use client'
import {useEffect,useState} from 'react';import AuthGate from '@/components/AuthGate';import {supabase} from '@/lib/supabase'
function pace(min:number,km:number){if(!km)return '—';const p=min/km,m=Math.floor(p),s=Math.round((p-m)*60);return `${m}:${String(s===60?0:s).padStart(2,'0')} min/km`}export default function Page(){const[rows,setRows]=useState<any[]>([]),[km,setKm]=useState(''),[min,setMin]=useState(''),[elev,setElev]=useState(''),[kcal,setKcal]=useState(''),[effort,setEffort]=useState('Moderado'),[msg,setMsg]=useState('');async function load(){const{data}=await supabase.from('running_logs').select('*').order('run_at',{ascending:false}).limit(60);setRows(data||[])}useEffect(()=>{load()},[]);async function save(){if(+km<=0||+min<=0)return setMsg('Captura distancia y duración.');const{error}=await supabase.from('running_logs').insert({distance_km:+km,duration_min:+min,elevation_m:elev?+elev:null,calories:kcal?+kcal:null,notes:`Esfuerzo: ${effort}`});setMsg(error?error.message:'✓ Entrenamiento guardado');if(!error){setKm('');setMin('');setElev('');setKcal('');load()}}const total=rows.reduce((a,x)=>a+Number(x.distance_km),0);return <AuthGate><h1>🏃 Running</h1><div className="grid"><div className="card"><span className="muted">Km registrados</span><div className="kpi">{total.toFixed(1)}</div></div><div className="card"><span className="muted">Último ritmo</span><div className="kpi small">{rows[0]?pace(Number(rows[0].duration_min),Number(rows[0].distance_km)):'—'}</div></div><div className="card"><span className="muted">Meta</span><div className="kpi">30 km</div><span className="muted">Pico de Orizaba</span></div></div><div className="card section"><h3>Registrar carrera</h3><div className="form-row"><input className="input" type="number" step="0.01" value={km} onChange={e=>setKm(e.target.value)} placeholder="Distancia km"/><input className="input" type="number" step="0.1" value={min} onChange={e=>setMin(e.target.value)} placeholder="Duración min"/><input className="input" type="number" value={elev} onChange={e=>setElev(e.target.value)} placeholder="Desnivel m"/><input className="input" type="number" value={kcal} onChange={e=>setKcal(e.target.value)} placeholder="Calorías"/><select className="input" value={effort} onChange={e=>setEffort(e.target.value)}><option>Suave</option><option>Moderado</option><option>Fuerte</option><option>Máximo</option></select><button className="btn" onClick={save}>Guardar</button></div>{km&&min&&<p className="muted">Ritmo calculado: <b>{pace(+min,+km)}</b></p>}{msg&&<p className="status">{msg}</p>}</div><div className="card section"><h3>Historial</h3><div className="table-wrap"><table><thead><tr><th>Fecha</th><th>Distancia</th><th>Tiempo</th><th>Ritmo</th><th>Desnivel</th><th>Calorías</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{new Date(x.run_at).toLocaleDateString('es-MX')}</td><td>{x.distance_km} km</td><td>{x.duration_min} min</td><td>{pace(Number(x.duration_min),Number(x.distance_km))}</td><td>{x.elevation_m??'—'} m</td><td>{x.calories??'—'}</td></tr>)}</tbody></table></div></div></AuthGate>}